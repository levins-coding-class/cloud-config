# Hetzner Cloud API Token
HETZNER_API_TOKEN=your-api-token-here

# Name des Administrator-Accounts (z.B. "levin")
ADMIN_NAME=your-admin-name

# SSH Public Keys für den Admin-Account (einer pro Zeile)
# Diese Keys werden in cloud-config.yaml eingetragen
SSH_AUTHORIZED_KEYS="ssh-ed25519 AAAA... your-key-comment
ssh-rsa AAAA... another-key"

# Optional: Hetzner SSH Key ID (für Rescue-System)
# Findest du unter: Hetzner Cloud Console → Security → SSH Keys
# HETZNER_SSH_KEY_ID=123456789
